import React, { component } from 'react';
import Controls from './Controls.js';

export default class Main extends Component {
  render() {
    return (
      <div>
        //whatever component goes next
      </div>
    )
  }
}